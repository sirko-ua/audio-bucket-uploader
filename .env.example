# Copy to .env and fill in your values. .env is git-ignored.
# Audio Bucket user API key (sent as a bearer token).
API_KEY=your_api_key_here
# Uploader endpoint URL.
API_URL=https://ukrab.work/api/uploader
# Optional: default input path (a .mkv file or a directory scanned recursively).
INPUT=/input
